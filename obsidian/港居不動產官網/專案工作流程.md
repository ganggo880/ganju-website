---
專案: 港居不動產官網與線上工具集
建立日期: 2026-07-25
層級: L3（Obsidian 詳細脈絡）
標籤: [專案, 港居不動產, 官網, 靜態網站]
---

# 港居不動產官網 — 專案工作流程

> L3 詳細脈絡層。精簡交接資訊看專案資料夾的 `handoff.md`，開發規範看 `AGENTS.md`。
> 本檔記錄「**為什麼這樣做**」——決策理由、踩過的坑、素材出處。

---

## 🎯 專案背景與脈絡

港居不動產開發有限公司（統編 `96797997`）主營**包租代管**業務。專案目的不只是做一個形象官網，而是把日常業務中反覆手算、手工處理的事情，變成房東與租客都能自己開來用的線上工具：

1. **官網主頁**：把「包租 vs 代管」兩種方案的差異講清楚，並用 ROI 試算器讓房東當場算出預估收益，直接引導填 Lead 表單。
2. **租金換算工具**：包租代管最常見的爭議來源就是「跨月入住的租金怎麼拆」與「10% 代管服務費怎麼算」。工具把三種計算基準（30 日 / 當月實際天數 / 自訂）全部支援，避免口頭解釋不清。
3. **檔案轉換神器**：租賃合約、房源照片、身分文件常需轉檔，外部轉檔網站有隱私疑慮 → 做成**純瀏覽器端本地運算**。
4. **照片水印工具**：巡檢與招租照需要可信的拍攝時間戳記，用 EXIF 讀取＋Canvas 合成解決。

整體技術定位：**純靜態、零後端、零部署成本**，發布在 GitHub Pages。

---

## 🧭 決策紀錄（為什麼這樣做）

| 決策 | 理由 |
|---|---|
| 純靜態網站，不做後端 | 業務規模不需要資料庫；GitHub Pages 免費、免維運、免資安顧慮。表單走 Lead 收集即可。 |
| 轉檔／水印全部在瀏覽器端跑（Canvas / Blob） | 合約與證件屬敏感文件，檔案**完全不上傳**，從架構上消除外洩風險，也不用付伺服器費用。 |
| 抽出 `css/tokens.css` ＋ `css/components.css` | 第一版 4 個頁面各自死寫色碼，改個品牌色要動 4 份檔案。抽 Token 後改一處全站生效，也才有辦法做深淺模式。 |
| 抽出 `js/layout.js` | 導覽列、Mobile Drawer、Active 點亮邏輯在每頁重複貼一次，維護成本高 → 統一成跨頁 Layout 控制器。 |
| 全站補 Schema.org JSON-LD | 主頁用 `RealEstateAgent`、租金工具用 `FinancialCalculator`、轉檔工具用 `WebApplication`。目的是搶搜尋結果的複合式摘要，工具頁本身就是自然流量入口。 |
| 專案藍圖用 `AGENTS.md` 而非只有 `CLAUDE.md` | AGENTS.md 是跨 Agent 開放標準，Codex / Gemini CLI / OpenCode 都讀得懂；換 Agent 接手不用重寫。 |
| 觸控區域強制 ≥ 44×44px | 目標使用者（房東、租客）以手機瀏覽為主，這是 Apple HIG 的最小可靠點擊尺寸。 |

---

## 🕳️ 踩坑筆記

- **`gh` CLI 這台電腦沒裝**：`gh auth status` 直接 command not found。GitHub 操作一律用原生 `git`；remote 早已設定完成，不需要（也不要）重建 repo。
- **Google 雲端硬碟上跑 git**：必須設 `git config windows.appendAtomically false`，否則 GDrive 的檔案鎖會造成 git 寫入失敗。已在 2026-07-25 設定完成。
- **GDrive 同步時機**：換電腦開工前一定要確認桌面版同步圖示已打勾，否則會拿到舊版檔案，最慘是造成 git 衝突。
- **`AGENTS.md` 曾是空殼**：初始化前檔案內只有一行 `CLAUDE.md`，任何 Agent 讀了等於沒讀。2026-07-25 已擴寫為完整藍圖。
- **色碼死寫的教訓**：重構前 4 份 CSS 各寫各的海軍藍，深淺不一致。之後任何新頁面**一律先引入 tokens.css**，不要圖快直接寫 hex。

---

## 🔗 素材與相關筆記

- 專案根目錄：`g:\我的雲端硬碟\ai agent\Matt Pocock skills`
- GitHub repo：https://github.com/ganggo880/ganju-website.git （main 分支）
- 線上網址：https://ganggo880.github.io/ganju-website/
- 系統技術規格書：`output/SPECIFICATION.md`
- 需求規格書（RFP）：`output/高優質住宅包租代管官方網站建置需求規格書_完整修訂版(RFP).md`
- 每日收工筆記：[[2026-07-25_工作筆記_港居不動產官網與RFP專案]]、[[2026-07-23_工作筆記_港居不動產官網與RFP專案]]

---

## 🗓️ 最近更動紀錄

| 日期 | 更動內容 | 更新者 |
|---|---|---|
| 2026-07-25 | **專案三層級初始化**：`AGENTS.md` 擴寫為完整藍圖、`handoff.md` 補標準交接欄位、`.gitignore` 加敏感檔封鎖、設定 GDrive 專用 git config、建立本 L3 筆記 | Claude Code (Opus 5) @ KEN-PC |
| 2026-07-25 | 完善 `CLAUDE.md` 與新增 `handoff.md`（commit `e7a6460`） | — |
| 2026-07-25 | 全站架構二次深度重構：`js/layout.js`、全站 Token 繼承、Schema.org、圖片 Lazy Loading（commit `29fcd5a`） | — |
| 2026-07-25 | 新增全站系統技術規格書 `output/SPECIFICATION.md`（commit `f9a7947`） | — |
| 2026-07-23 | 線上檔案轉換神器 `file-converter.html` 整合完成（commit `ada5087`） | — |
