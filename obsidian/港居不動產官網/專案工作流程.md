---
專案: 港居不動產官網與線上工具集
建立日期: 2026-07-25
層級: L3（Obsidian 詳細脈絡）
標籤: [專案, 港居不動產, 官網, 靜態網站]
---

# 港居不動產官網 — 專案工作流程

> L3 詳細脈絡層。精簡交接資訊看專案資料夾的 `handoff.md`，開發規範看 `AGENTS.md`。
> 本檔記錄「**為什麼這樣做**」——決策理由、踩過的坑、素材出處。

---

## 🎯 專案背景與脈絡

港居不動產開發有限公司（統編 `96797997`）主營**包租代管**業務。專案目的不只是做一個形象官網，而是把日常業務中反覆手算、手工處理的事情，變成房東與租客都能自己開來用的線上工具：

1. **官網主頁**：把「包租 vs 代管」兩種方案的差異講清楚，並用 ROI 試算器讓房東當場算出預估收益，直接引導填 Lead 表單。
2. **租金換算工具**：包租代管最常見的爭議來源就是「跨月入住的租金怎麼拆」與「10% 代管服務費怎麼算」。工具把三種計算基準（30 日 / 當月實際天數 / 自訂）全部支援，避免口頭解釋不清。
3. **檔案轉換神器**：租賃合約、房源照片、身分文件常需轉檔，外部轉檔網站有隱私疑慮 → 做成**純瀏覽器端本地運算**。
4. **照片水印工具**：巡檢與招租照需要可信的拍攝時間戳記，用 EXIF 讀取＋Canvas 合成解決。

整體技術定位：**純靜態、零後端、零部署成本**，發布在 GitHub Pages。

---

## 🧭 決策紀錄（為什麼這樣做）

| 決策 | 理由 |
|---|---|
| 純靜態網站，不做後端 | 業務規模不需要資料庫；GitHub Pages 免費、免維運、免資安顧慮。表單走 Lead 收集即可。 |
| 轉檔／水印全部在瀏覽器端跑（Canvas / Blob） | 合約與證件屬敏感文件，檔案**完全不上傳**，從架構上消除外洩風險，也不用付伺服器費用。 |
| 抽出 `css/tokens.css` ＋ `css/components.css` | 第一版 4 個頁面各自死寫色碼，改個品牌色要動 4 份檔案。抽 Token 後改一處全站生效，也才有辦法做深淺模式。 |
| 抽出 `js/layout.js` | 導覽列、Mobile Drawer、Active 點亮邏輯在每頁重複貼一次，維護成本高 → 統一成跨頁 Layout 控制器。 |
| 全站補 Schema.org JSON-LD | 主頁用 `RealEstateAgent`、租金工具用 `FinancialCalculator`、轉檔工具用 `WebApplication`。目的是搶搜尋結果的複合式摘要，工具頁本身就是自然流量入口。 |
| 專案藍圖用 `AGENTS.md` 而非只有 `CLAUDE.md` | AGENTS.md 是跨 Agent 開放標準，Codex / Gemini CLI / OpenCode 都讀得懂；換 Agent 接手不用重寫。 |
| 觸控區域強制 ≥ 44×44px | 目標使用者（房東、租客）以手機瀏覽為主，這是 Apple HIG 的最小可靠點擊尺寸。 |
| 44px 熱區只在 `(pointer: coarse), (max-width: 992px)` 生效 | 若無條件套用到桌機，頁尾連結會從 18px 拉高到 44px，footer 整體高度暴增約 100px，等於未經同意改掉桌機版面密度。觸控裝置才是這條規則真正要保護的對象。 |
| 共用組件的 RWD `@media` 一律寫在 `components.css` | 原本 Drawer 與 `.mobile-toggle` 的響應式規則只寫在 `styles.css`，而 `styles.css` 只有 `index.html` 載入 → 三個工具頁在手機上完全沒有導覽入口、頁面橫向溢出到 930～946px。這是「抽出公用組件時只抽了靜態樣式、忘了抽響應式規則」的典型後果。 |
| `FinancialCalculator` 改用 `WebApplication` + `applicationCategory: FinanceApplication` | 查證 schema.org/FinancialCalculator 回 **404**——這個型別根本不存在，Google Rich Results Test 會判為無效物件型別。正確做法是用 `WebApplication` 搭配 `applicationCategory`。 |
| 三個工具頁補 `offers`，但**不補** `aggregateRating` | Google 對 SoftwareApplication rich result 要求 `name` + `offers.price` + (`aggregateRating` 或 `review`)。工具真的免費，所以 `offers` 填 TWD 0 是事實；但評分需要真實使用者評價，寧可拿不到 rich result 也不編造。 |
| 首頁刻意不填 `priceRange`／`openingHoursSpecification` | 這兩項屬 LocalBusiness 的建議欄位，能提升在地商家結果的完整度，但實際價格帶與營業時間我沒有可信來源。結構化資料造假會被 Google 視為 spam，寧缺勿假——**等老闆提供真實營業時間再補**。 |
| 字型從 CSS `@import` 改為 HTML `<link>` | `@import` 造成「HTML → styles.css → fonts.googleapis.com」的串聯阻塞請求鏈，明顯拖慢 FCP/LCP。順帶發現 `rent-calculator.html` 與 `file-converter.html` 有 `preconnect` 到 fonts.googleapis.com 卻**從未真的載入任何字型檔**（字型 `@import` 只寫在 index 專用的 styles.css）——等於白做 preconnect 且字型 fallback 到系統字型。 |

---

## 🕳️ 踩坑筆記

- **`gh` CLI 這台電腦沒裝**：`gh auth status` 直接 command not found。GitHub 操作一律用原生 `git`；remote 早已設定完成，不需要（也不要）重建 repo。
- **Google 雲端硬碟上跑 git**：必須設 `git config windows.appendAtomically false`，否則 GDrive 的檔案鎖會造成 git 寫入失敗。已在 2026-07-25 設定完成。
- **GDrive 同步時機**：換電腦開工前一定要確認桌面版同步圖示已打勾，否則會拿到舊版檔案，最慘是造成 git 衝突。
- **`AGENTS.md` 曾是空殼**：初始化前檔案內只有一行 `CLAUDE.md`，任何 Agent 讀了等於沒讀。2026-07-25 已擴寫為完整藍圖。
- **色碼死寫的教訓**：重構前 4 份 CSS 各寫各的海軍藍，深淺不一致。之後任何新頁面**一律先引入 tokens.css**，不要圖快直接寫 hex。

### 🕳️ CSS 與驗證流程的坑（2026-07-26 新增）

- **`@import` 的層疊順序陷阱（本次卡最久的一題）**：每個頁面 CSS 都在檔首 `@import url('css/components.css')`。CSS 規範下，被 import 的規則會被插在**該檔案所有規則之前**。所以 `styles.css` 後面只要再寫一次 `.nav-menu { display: flex }` 或 `.mobile-toggle { display: none }`，就會以「同權重、後出現者勝」蓋掉 `components.css` 裡 `@media` 內的響應式宣告。
  - 症狀：改完 `components.css` 的 `@media`，工具頁生效了，`index.html` 卻沒生效（因為只有 index 載入 styles.css）。
  - 解法：把 `styles.css` / `watermark.css` 裡重複宣告的 `.nav-menu`、`.nav-link`、`.mobile-toggle` 刪掉，只保留頁面專屬微調，並在原處留註解警告後人不要再宣告 `display`。
- **Python `http.server` 不送 no-cache，量測會騙人**：改完 CSS 後重新載入頁面，瀏覽器仍沿用記憶體中的舊 CSS，導致「明明檔案已改好、`fetch()` 讀到的是新內容，但 `getComputedStyle` 還是舊行為」。判斷方法：檢查 `document.styleSheets` 裡 components.css 的 `@media` 規則數是否為 0。
  - 解法：驗證前換一個 port 重啟 preview server（改 `.claude/launch.json` 的 port），新 origin 就拿不到舊快取。
- **本機沒有 Node/npx/lighthouse CLI**：`gh` 也沒裝。所以 Lighthouse 跑不起來，只能用瀏覽器內腳本做等效稽核（量測所有互動元素的 `getBoundingClientRect`、檢查可及名稱與 label 關聯、圖片屬性、canonical、JSON-LD 解析）。正式分數要靠 PageSpeed Insights 線上補。
- **iframe 量測法**：Browser pane 的 `resize_window` 在某些分頁會卡住不生效（`innerWidth` 不變）。改用「在頁面內建立指定寬度的 iframe 載入目標頁」來量測，媒體查詢會依 iframe 寬度生效，結果等價且可一次跑完 4 頁 × 3 斷點。但要注意：外層視窗處於行動裝置模擬時，iframe 寬度會被夾到視窗寬度，必須先把外層設回桌機寬度。
- **`.claude/` 被納入版控 → git 直接癱瘓（2026-07-26 踩到）**：`.claude/launch.json`（preview server 設定）被 commit 進分支後，該目錄在本機是系統保護狀態，`ls`／`Test-Path`／`attrib` 全部 Access denied。結果 `git checkout` 與 `git merge` 一律失敗於 `cannot stat '.claude': Permission denied`，連切回分支都做不到，worktree 一度被留在舊版 main 內容上。
  - 嘗試過無效的做法：`git sparse-checkout set --no-cone '/*' '!/.claude/'`（git 仍會去 stat 該路徑）。
  - 有效解法：`git checkout <branch> -- ':(exclude).claude'` 把分支內容帶進當前分支的 worktree＋index，再單一 commit；同時把 `.claude/` 寫進 `.gitignore` 永久排除。
  - 教訓：**工具鏈的本機設定檔（`.claude/`、`.vscode/` 之類）不要進版控**，收工技能本來就有這條規則，這次是它被 commit 進去才炸的。
- **量測誤判：核取方塊 22×22px 不算違規**：`<input type="checkbox">` 本體只有 22px，但外層 `<label>` 已有 `min-height: 44px` 且包住文字，點 label 就會切換 checkbox → 有效觸控目標其實是整個 label（>44px）。腳本只量 input 本身會誤報。

---

## 🔗 素材與相關筆記

- 專案根目錄：`g:\我的雲端硬碟\ai agent\Matt Pocock skills`
- GitHub repo：https://github.com/ganggo880/ganju-website.git （main 分支）
- 線上網址：https://ganggo880.github.io/ganju-website/
- 系統技術規格書：`output/SPECIFICATION.md`
- 需求規格書（RFP）：`output/高優質住宅包租代管官方網站建置需求規格書_完整修訂版(RFP).md`
- 每日收工筆記：[[2026-07-25_工作筆記_港居不動產官網與RFP專案]]、[[2026-07-23_工作筆記_港居不動產官網與RFP專案]]

---

## 🗓️ 最近更動紀錄

| 日期 | 更動內容 | 更新者 |
|---|---|---|
| 2026-07-26 | **SEO／無障礙／效能／RWD 優化 + JSON-LD 校正**：共用組件 RWD 規則歸位 `components.css`（修好三個工具頁手機版無導覽、橫向溢出 930～946px）、`photo-watermark.html` 補漢堡鈕、`FinancialCalculator` 改 `WebApplication`、補 `offers`／canonical／絕對 og:image／aria-label／`rel="noopener"`／圖片尺寸與 lazy、字型改 HTML `<link>`。**未完成**：轉檔頁 360px 仍溢出 660px、租金頁溢出 9px。分支 `feat/seo-a11y-rwd-optimize` → 合併回 `main` | Claude Code (Opus 5) @ KEN-PC |
| 2026-07-26 | 新增 `id-card-watermark.html` 身份證照裁剪與租賃水印工具，並修正七項缺陷 | Claude Code (Opus 5) @ KEN-PC |
| 2026-07-25 | **專案三層級初始化**：`AGENTS.md` 擴寫為完整藍圖、`handoff.md` 補標準交接欄位、`.gitignore` 加敏感檔封鎖、設定 GDrive 專用 git config、建立本 L3 筆記 | Claude Code (Opus 5) @ KEN-PC |
| 2026-07-25 | 完善 `CLAUDE.md` 與新增 `handoff.md`（commit `e7a6460`） | — |
| 2026-07-25 | 全站架構二次深度重構：`js/layout.js`、全站 Token 繼承、Schema.org、圖片 Lazy Loading（commit `29fcd5a`） | — |
| 2026-07-25 | 新增全站系統技術規格書 `output/SPECIFICATION.md`（commit `f9a7947`） | — |
| 2026-07-23 | 線上檔案轉換神器 `file-converter.html` 整合完成（commit `ada5087`） | — |
